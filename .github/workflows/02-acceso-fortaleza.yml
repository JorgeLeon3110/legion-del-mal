name: Acceso a la fortaleza

on: 
  workflow_dispatch :
#push 

jobs:
  acceso-a-Fortaleza:
    runs-on: ubuntu-latest

    steps:
      - name: El agente llega a la fortaleza
        run: | 
          echo "Agente ${{ github.actor}} llega a la fortaleza"
          echo ""
          echo " buscar los archivos de la legion"
          ls -la
          echo ""
          echo " Pero que paso no hay nada "
      
      - name: Brianiac le da acceso 
        uses: actions/checkout@v4

      - name: Ahora si vamos a revisar los archivos de la legion  
        run: | 
          ls -la
          echo "funciona"
          echo " " 
          echo " Planes "
          ls planes/
          echo " Villanos "
          ls miembros/
          echo " Inteligencia "
          ls inteligencia/
